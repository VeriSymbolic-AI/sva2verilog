---
phase: 02-verilator-parity-ci-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
files_modified:
  - .github/workflows/ci.yml
  - tests/test_sequential.py
autonomous: true
requirements:
  - VALIDATE-03

must_haves:
  truths:
    - "CI matrix has 8 jobs: 2 OS × 2 Python × 2 simulators"
    - "Verilator jobs only run simulation tests, not all 736 tests"
    - "lint job is unchanged"
    - "test_verilator_lint_clean runs on Verilator axis, not skipped"
    - "fail-fast: false preserved"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "3-axis CI matrix with simulator dimension"
      contains: "simulator: [iverilog, verilator]"
      contains: "matrix.simulator"
      contains: "apt-get install -y verilator"
      contains: "brew install verilator"
    - path: "tests/test_sequential.py"
      provides: "Upgraded verilator lint test"
      contains: "test_verilator_lint_clean"
  key_links:
    - from: "ci.yml test job"
      to: "pytest --simulator=${{ matrix.simulator }}"
      via: "matrix.simulator variable"
      pattern: "matrix\\.simulator"
    - from: "ci.yml conditional install"
      to: "apt-get/brew verilator"
      via: "if: matrix.simulator == 'verilator'"
      pattern: "matrix\\.simulator.*verilator"
    - from: "test_verilator_lint_clean"
      to: "verilator --lint-only"
      via: "subprocess.run"
      pattern: "verilator.*--lint-only"
---

<objective>
Expand the CI matrix to 8 jobs (Ubuntu/macOS × Python 3.12/3.13 × {iverilog, Verilator}) and upgrade `test_verilator_lint_clean` from skipped to actively running on the Verilator axis.

Per D-05: extend existing `test` job matrix with `simulator` axis → 8 jobs. Single `test` job, conditional install steps. `lint` job unchanged. Per D-08: `test_verilator_lint_clean` upgrade from skip to run on Verilator axis. Per D-06: no soft-fail — divergence = CI failure that must be fixed in Phase 2.

Purpose: Establish the dual-oracle CI gate so all subsequent hardening phases (3-5) are automatically validated under both simulators.
Output: `.github/workflows/ci.yml` updated, `tests/test_sequential.py` updated.
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-verilator-parity-ci-expansion/02-RESEARCH.md
@.planning/phases/02-verilator-parity-ci-expansion/02-CONTEXT.md

# Codebase files to read before implementing:
@.github/workflows/ci.yml
@tests/test_sequential.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add simulator axis to CI test matrix with conditional Verilator install</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Per D-05 and RESEARCH.md Pattern 3: expand the existing `test` job matrix to include a `simulator` dimension.

    **Matrix changes:**
    Add `simulator: [iverilog, verilator]` to the existing matrix under the `test` job. The resulting matrix: `os × python × simulator` = 2 × 2 × 2 = 8 jobs.

    **Conditional simulator installation:**

    The existing CI has `Install iverilog (Linux)` and `Install iverilog (macOS)` steps. These are currently unconditional (they always install iverilog). Modify them:

    - **iverilog (Linux):** Add `if: matrix.simulator == 'iverilog' && runner.os == 'Linux'`
    - **iverilog (macOS):** Add `if: matrix.simulator == 'iverilog' && runner.os == 'macOS'`

    Add new steps:

    - **Verilator (Linux):**
      ```yaml
      - name: Install Verilator (Linux)
        if: matrix.simulator == 'verilator' && runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y verilator
      ```

    - **Verilator (macOS):**
      ```yaml
      - name: Install Verilator (macOS)
        if: matrix.simulator == 'verilator' && runner.os == 'macOS'
        run: brew install verilator
      ```

    Per D-01: `apt-get install -y verilator` (Ubuntu), `brew install verilator` (macOS), no version pinning.

    **Test execution changes:**

    Modify the `Run tests` step to pass `--simulator=${{ matrix.simulator }}` and restrict to simulation-marked tests for Verilator jobs:

    ```yaml
    - name: Run tests
      run: uv run pytest tests/ -m simulation --simulator=${{ matrix.simulator }} -v --timeout=120
      env:
        SLANG_PATH: /usr/local/bin/slang
    ```

    Wait — this would make iverilog jobs also run only `-m simulation`. That's wrong. The iverilog jobs should continue running ALL tests.

    **Correct approach:** Use conditional test commands:

    ```yaml
    - name: Run tests (iverilog — full suite)
      if: matrix.simulator == 'iverilog'
      run: uv run pytest tests/ -v --timeout=120
      env:
        SLANG_PATH: /usr/local/bin/slang

    - name: Run tests (Verilator — simulation only)
      if: matrix.simulator == 'verilator'
      run: uv run pytest tests/ -m simulation --simulator=verilator -v --timeout=120
      env:
        SLANG_PATH: /usr/local/bin/slang
    ```

    Per D-02: only `simulation`-marked tests (~65) run on Verilator axis.

    **IMPORTANT — Preserve existing settings:**
    - `fail-fast: false` stays
    - `lint` job is NOT modified (ruff + mypy don't need simulators)
    - slang installation steps are NOT modified (needed on all jobs for parsing)
    - All existing steps before the test execution step stay in order
    - `SLANG_PATH` env var stays

    **No exclude rules needed** — both simulators are available on both OSes via package managers.
  </action>
  <verify>
    <automated>cd /Users/allenenli/Documents/formal_sva_rtl && python -c "
import yaml
with open('.github/workflows/ci.yml') as f:
    ci = yaml.safe_load(f)
test_job = ci['jobs']['test']
matrix = test_job['strategy']['matrix']
assert 'simulator' in matrix, 'simulator axis missing'
assert matrix['simulator'] == ['iverilog', 'verilator'], f'Expected [iverilog, verilator], got {matrix[\"simulator\"]}'
assert test_job['strategy']['fail-fast'] == False
print('CI matrix validation OK')
"</automated>
  </verify>
  <done>ci.yml has simulator axis in test matrix; conditional install steps for both simulators; iverilog jobs run full suite, Verilator jobs run -m simulation; lint job unchanged; YAML is valid</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade test_verilator_lint_clean from skip to run on Verilator axis</name>
  <files>tests/test_sequential.py</files>
  <action>
    Per D-08: upgrade `test_verilator_lint_clean` from skipped to actively running on the Verilator CI axis.

    Current state: The test uses `@pytest.mark.skipif(not _HAS_VERILATOR, ...)` which skips when verilator is not on PATH. This is correct behavior — the test should still skip when verilator is not installed (e.g., on iverilog CI jobs or local dev without verilator).

    The upgrade: the test is already correctly structured. The CI Verilator jobs will have verilator installed (via apt-get/brew), so the `skipif` will NOT trigger, and the test will run. No code change is needed in the test itself.

    However, we need to ensure the test is collected on the Verilator axis. Currently it's in `tests/test_sequential.py` which is NOT under `tests/simulation/`. The Verilator CI jobs run `pytest tests/ -m simulation`. This test is NOT marked `@pytest.mark.simulation`.

    **Action:** Add `@pytest.mark.simulation` marker to `test_verilator_lint_clean` so it's collected when running `-m simulation`. This is appropriate because the test requires verilator (a simulator tool) to run.

    ```python
    @pytest.mark.simulation
    @pytest.mark.skipif(not _HAS_VERILATOR, reason="verilator not installed")
    @pytest.mark.parametrize("fixture_name", _PHASE2_FIXTURES)
    def test_verilator_lint_clean(fixture_name: str) -> None:
    ```

    Also, add `import pytest` at the top of the file if not already present (it should be — verify).

    Per D-02: the test scope is correct — `verilator --lint-only` on generated RTL. No changes to the test body needed.
  </action>
  <verify>
    <automated>cd /Users/allenenli/Documents/formal_sva_rtl && uv run pytest tests/test_sequential.py::test_verilator_lint_clean --collect-only -q 2>&1 | head -10</automated>
  </verify>
  <done>test_verilator_lint_clean has @pytest.mark.simulation marker; test is collected by `pytest -m simulation`; test still skips when verilator not on PATH; test body unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Verify CI configuration completeness and local simulation marker test</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Verify the CI configuration is complete and consistent:

    Step 1 — Validate YAML syntax:
    ```
    python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
    ```

    Step 2 — Verify key properties:
    - 8 matrix combinations: 2 OS × 2 Python × 2 simulators
    - `fail-fast: false` preserved
    - `lint` job has exactly the same steps as before (no simulator added)
    - slang is installed on ALL jobs (both iverilog and Verilator)
    - Verilator install steps have correct `if:` conditions
    - iverilog install steps have `if: matrix.simulator == 'iverilog'` conditions
    - Iverilog test step runs full suite (`pytest tests/`)
    - Verilator test step runs only simulation (`pytest tests/ -m simulation --simulator=verilator`)
    - Timeout is 120s

    Step 3 — Verify simulation marker coverage:
    ```
    uv run pytest tests/ -m simulation --collect-only -q
    ```
    Confirm that all expected simulation tests are collected (including test_verilator_lint_clean now). The count should be ~65 + the parametrized lint test instances.

    Step 4 — Run a quick local verification that the iverilog path still works:
    ```
    uv run pytest tests/simulation/test_sim_rose.py -m simulation --simulator=iverilog -v --timeout=120
    ```
  </action>
  <verify>
    <automated>cd /Users/allenenli/Documents/formal_sva_rtl && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('YAML valid')" && uv run pytest tests/simulation/test_sim_rose.py -m simulation --simulator=iverilog -v --timeout=120 2>&1 | tail -5</automated>
  </verify>
  <done>CI YAML is valid; simulation marker coverage confirmed; iverilog smoke test passes; all 8 CI matrix combinations are correctly configured</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions workflow → apt/brew | Package installation commands cross from CI config into system package manager |
| CI matrix variable → shell command | `${{ matrix.simulator }}` interpolated into shell commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | matrix.simulator interpolation | mitigate | GitHub Actions matrix values are statically defined in YAML; `simulator: [iverilog, verilator]` — no user-controlled input |
| T-02-12 | Spoofing | brew install verilator | accept | Homebrew core formula is maintained by Homebrew project; well-known package with 20+ year history |
| T-02-13 | Denial of Service | CI resource exhaustion | accept | 8 jobs is within GitHub Actions free tier limits (20 concurrent jobs for public repos); fail-fast: false prevents one failure from cancelling others |
| T-02-14 | Information Disclosure | CI logs | accept | Simulation stdout contains only 0/1 output; no secrets |
| T-02-SC | Tampering | apt/brew packages | accept | System package manager verifies package signatures; verilator and iverilog are well-known packages |
</threat_model>

<verification>
## Phase-level verification

After Plan 04 executes:
```bash
# Validate CI YAML
python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('OK')"

# Verify simulation marker includes lint test
uv run pytest tests/ -m simulation --collect-only -q | grep verilator_lint_clean

# Iverilog path still works
uv run pytest tests/simulation/test_sim_rose.py -m simulation --simulator=iverilog -v --timeout=120
```

Push to trigger CI and verify all 8 jobs are green.
</verification>

<success_criteria>
1. CI matrix produces exactly 8 test jobs: Ubuntu/macOS × Py 3.12/3.13 × {iverilog, Verilator}
2. Iverilog jobs run the full 736-test suite and pass
3. Verilator jobs run only simulation-marked tests and pass
4. `test_verilator_lint_clean` runs (not skipped) on Verilator jobs
5. `lint` job is unchanged
6. All 8 jobs green on the PR that introduces these changes
</success_criteria>

<output>
Create `.planning/phases/02-verilator-parity-ci-expansion/02-04-SUMMARY.md` when done
</output>
