---
phase: 02-verilator-parity-ci-expansion
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-04
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements:
  - VALIDATE-04

must_haves:
  truths:
    - "README documents Verilator as a supported simulator alongside iverilog"
    - "README includes Verilator install instructions for Ubuntu and macOS"
    - "README documents --simulator flag usage"
    - "CLAUDE.md documents the dual-oracle validation contract"
  artifacts:
    - path: "README.md"
      provides: "User-facing Verilator documentation"
      contains: "Verilator"
      contains: "brew install verilator"
      contains: "apt-get install verilator"
      contains: "--simulator"
    - path: "CLAUDE.md"
      provides: "AI-agent dual-oracle contract"
      contains: "dual-oracle"
      contains: "Verilator"
      contains: "iverilog"
  key_links:
    - from: "README.md install section"
      to: "apt-get / brew verilator commands"
      via: "documentation"
      pattern: "verilator"
    - from: "CLAUDE.md validation constraint"
      to: "CI matrix (iverilog + Verilator)"
      via: "dual-oracle contract text"
      pattern: "dual.oracle"
---

<objective>
Document the dual-oracle commitment: update README.md with Verilator install instructions and `--simulator` flag usage, and update CLAUDE.md with the dual-oracle validation contract for AI agents.

Per D-09: README (Verilator install + `--simulator` flag), CLAUDE.md (dual oracle contract). Per VALIDATE-04: dual-oracle commitment documented in README and enforced in CI.

Purpose: Ensure users and AI agents understand that all generated monitors are validated under two independent simulators.
Output: `README.md` and `CLAUDE.md` updated with Verilator documentation.
</objective>

<execution_context>
@$HOME/.codebuddy/get-shit-done/workflows/execute-plan.md
@$HOME/.codebuddy/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-verilator-parity-ci-expansion/02-CONTEXT.md
@.planning/phases/02-verilator-parity-ci-expansion/02-RESEARCH.md

# Codebase files to read before implementing:
@README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Verilator documentation to README.md</name>
  <files>README.md</files>
  <action>
    Per D-09 and VALIDATE-04: update README.md to document Verilator as a supported simulator.

    First, read README.md to understand its current structure. Find the appropriate section(s) to add Verilator documentation. If there's an existing "Simulation" or "Getting Started" or "Prerequisites" section, add Verilator content there. If not, create appropriate sections.

    **Content to add:**

    1. **Prerequisites section (or extend existing):** Add Verilator to the list of required tools alongside iverilog:
       - Ubuntu: `sudo apt-get install -y verilator`
       - macOS: `brew install verilator`
       - Note: Verilator 5.x is recommended (the version provided by your system package manager)
       - Per D-01: no version pinning — use the latest stable from package manager

    2. **Running simulation tests section (or create):** Document the `--simulator` flag:
       ```
       ## Running Simulation Tests

       sva2rtl uses a dual-oracle simulation strategy. Every generated monitor
       is validated under two independent simulators:

       - **Icarus Verilog (iverilog)** — the default simulator
       - **Verilator** — a high-performance SystemVerilog simulator

       Run simulation tests with a specific simulator:

           # Default (iverilog)
           pytest tests/ -m simulation

           # Verilator
           pytest tests/ -m simulation --simulator=verilator

           # Via environment variable
           SVA2RTL_SIMULATOR=verilator pytest tests/ -m simulation
       ```

    3. **CI section (update or add):** Note that CI runs both simulators:
       ```
       CI validates all monitors under both iverilog and Verilator across
       Ubuntu and macOS (8 jobs: 2 OS × 2 Python × 2 simulators).
       ```

    **Style guidelines:**
    - Match the existing README tone and formatting
    - Use the same heading levels and code block style as existing content
    - Keep instructions concise and actionable
    - Don't reorganize the entire README — insert content where it naturally fits
  </action>
  <verify>
    <automated>cd /Users/allenenli/Documents/formal_sva_rtl && grep -i "verilator" README.md | head -5</automated>
  </verify>
  <done>README.md contains Verilator install instructions for both Ubuntu and macOS; documents --simulator flag; mentions dual-oracle CI strategy; content is well-integrated with existing README structure</done>
</task>

<task type="auto">
  <name>Task 2: Add dual-oracle contract to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
    Per D-09 and VALIDATE-04: update CLAUDE.md with the dual-oracle validation contract.

    First, read CLAUDE.md to understand its current structure. The file already mentions "Validation: All generated monitors must pass equivalence checking against behavioral simulation (Icarus/Verilator)" — this is a good foundation.

    **Content to add or update:**

    Find the "Validation" or "Constraints" section and add/expand:

    ```
    ## Dual-Oracle Validation Contract

    Every generated hardware monitor MUST pass simulation under BOTH:

    1. **Icarus Verilog (iverilog)** — the baseline oracle
    2. **Verilator** — the independent verification oracle

    This is enforced in CI: all 8 matrix jobs (Ubuntu/macOS × Python 3.12/3.13
    × {iverilog, Verilator}) must be green before merge.

    ### Rules for AI agents:

    - Any code change that affects generated RTL MUST be validated under both
      simulators before claiming completion.
    - A test passing under iverilog but failing under Verilator is a defect
      that MUST be fixed — never waived or marked as xfail.
    - The `--simulator` pytest flag controls which backend is used:
      `--simulator=iverilog` (default) or `--simulator=verilator`.
    - Simulation tests live in `tests/simulation/` and are marked
      `@pytest.mark.simulation`.
    - The Verilator backend uses a C++ wrapper (`tests/simulation/wrapper.cpp.j2`)
      compiled with `verilator --exe --build --timing` (NOT `--binary`).
    ```

    **Style guidelines:**
    - Match existing CLAUDE.md tone (instructional, directed at AI agents)
    - Keep the contract clear and enforceable
    - Don't remove existing validation language — expand on it
  </action>
  <verify>
    <automated>cd /Users/allenenli/Documents/formal_sva_rtl && grep -i "dual.oracle\|dual-oracle" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md contains a "Dual-Oracle Validation Contract" section with clear rules for AI agents; mentions both simulators, CI enforcement, --simulator flag, and the Verilator backend architecture</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan — no code execution boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Information Disclosure | README.md | accept | Public documentation for an open-source project; no secrets |
| T-02-SC | Tampering | pip/cargo installs | accept | No dependencies changed in this plan |
</threat_model>

<verification>
## Phase-level verification

After Plan 05 executes:
```bash
# Verify README has Verilator content
grep -c "verilator\|Verilator" README.md

# Verify CLAUDE.md has dual-oracle contract
grep -c "dual-oracle\|Dual-Oracle" CLAUDE.md

# Verify install commands are present
grep "apt-get install.*verilator\|brew install verilator" README.md
```
</verification>

<success_criteria>
1. README.md includes Verilator install commands for Ubuntu and macOS
2. README.md documents `--simulator` flag with usage examples
3. README.md mentions dual-oracle CI strategy
4. CLAUDE.md contains explicit dual-oracle validation rules for AI agents
5. Both documents are consistent with the actual implementation (Plans 01-04)
</success_criteria>

<output>
Create `.planning/phases/02-verilator-parity-ci-expansion/02-05-SUMMARY.md` when done
</output>
