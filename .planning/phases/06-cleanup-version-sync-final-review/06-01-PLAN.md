---
phase: 06
plan: 01
wave: 1
depends_on: []
files_modified:
  - src/sva2rtl/__init__.py
  - pyproject.toml
  - SUPPORTED_CONSTRUCTS.md
  - .planning/PROJECT.md
  - .planning/phases/06-cleanup-version-sync-final-review/06-TRIAGE.md
  - .planning/phases/06-cleanup-version-sync-final-review/06-REVIEW.md
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
goal: Sync version to 1.1.0, triage all 19 MEDIUM/LOW findings, perform cross-phase code review confirming zero new HIGH findings.
---

# Plan 06-01: Version Sync + MEDIUM/LOW Triage + Cross-Phase Review

## Overview

Single plan covering all four POLISH requirements. All tasks are read-only or minimal-edit documentation work. Zero changes to `tests/` or `src/sva2rtl/*.py` (except version string + error message tweak).

Tasks 1-4 (version sync + doc fixes) are independent and can run in sequence. Task 5 (triage artifact) depends on CONTEXT.md decisions. Task 6 (code review) is independent of tasks 1-5.

## Threat Model

| Threat | Mitigation |
|--------|-----------|
| Version string 不一致 | 两个文件同时修改，同一 commit |
| Triage 遗漏 | 按 CONTEXT.md 的 19 项清单逐条核对 |
| Code review 漏掉新 HIGH | 对完整 v1.0..HEAD diff 逐文件审查 |
| 回归 | 736 tests before/after；没有 src/ 逻辑变更 |

## Tasks

<task id="6.1.1" title="POLISH-01 — Version sync to 1.1.0">
  <read_first>src/sva2rtl/__init__.py (version line), pyproject.toml (version line)</read_first>
  <action>
    Update `src/sva2rtl/__init__.py`: change `__version__` from `"0.1.0"` to `"1.1.0"`.
    Update `pyproject.toml`: change `version` from `"1.0.0"` to `"1.1.0"`.
    Verify: `python -c "import sva2rtl; print(sva2rtl.__version__)"` prints `1.1.0`.
    Commit: `chore(POLISH-01): sync version to 1.1.0`
  </action>
  <acceptance_criteria>Both files read `1.1.0`. Runtime import confirms.</acceptance_criteria>
</task>

<task id="6.1.2" title="POLISH — Fix M-06.4: remove --default-clock from error message">
  <read_first>Search for `--default-clock` in src/sva2rtl/ directory</read_first>
  <action>
    Find the error message referencing `--default-clock` (flag does not exist in CLI).
    Remove the reference. Do not change error semantics.
    Commit: `fix(POLISH): remove --default-clock reference from error message (M-06.4)`
  </action>
  <acceptance_criteria>Zero hits for `--default-clock` in src/. Error message still informative.</acceptance_criteria>
</task>

<task id="6.1.3" title="POLISH — Fix M-06.5: update SVA-E005 description in SUPPORTED_CONSTRUCTS.md">
  <read_first>SUPPORTED_CONSTRUCTS.md, search for SVA-E005</read_first>
  <action>
    Update the SVA-E005 description in SUPPORTED_CONSTRUCTS.md to match actual error behavior.
    Commit: `docs(POLISH): update SVA-E005 description (M-06.5)`
  </action>
  <acceptance_criteria>SVA-E005 docs match actual error output.</acceptance_criteria>
</task>

<task id="6.1.4" title="POLISH — Fix L-06.6: update attempt_fired description in SUPPORTED_CONSTRUCTS.md">
  <read_first>SUPPORTED_CONSTRUCTS.md, search for attempt_fired</read_first>
  <action>
    Update the attempt_fired description from "pulse" to "sticky" (HARDEN-01 semantics).
    Commit: `docs(POLISH): update attempt_fired description to sticky semantics (L-06.6)`
  </action>
  <acceptance_criteria>SUPPORTED_CONSTRUCTS.md describes attempt_fired as sticky.</acceptance_criteria>
</task>

<task id="6.1.5" title="POLISH-02/03 — Write MEDIUM/LOW triage artifact">
  <read_first>.planning/phases/06-cleanup-version-sync-final-review/06-CONTEXT.md (triage decisions section)</read_first>
  <action>
    Write `.planning/phases/06-cleanup-version-sync-final-review/06-TRIAGE.md` listing all 19 findings (10 MEDIUM + 9 LOW) with dispositions per CONTEXT.md:

    **Closed (2):**
    - M-06.7 (slang version docs): closed — v7.0 documented in ROADMAP/CONTEXT
    - L-06.5 (bool_expr attempt_fired_q / disable_i): closed — HARDEN-01 fix in Phase 3

    **Fixed in this phase (4):**
    - M-06.4 (--default-clock ref): fixed — commit SHA from task 6.1.2
    - M-06.5 (SVA-E005 docs): fixed — commit SHA from task 6.1.3
    - L-06.6 (attempt_fired docs): fixed — commit SHA from task 6.1.4
    - M-06.3 (dump-ir separator): partially fixed in Phase 5 (\n\n separator added)

    **Fixed in prior phases (5):**
    - L-06.3 (seq_concat_top wire): fixed — Phase 3 template refactor
    - L-06.4 (concat_delay zero-delay): fixed — Phase 3 template refactor
    - L-06.7 (repeated boilerplate): mostly fixed — Phase 3 macro extraction (-289 lines)
    - M-06.6 (--verilog CLI snapshot): addressed — Phase 2 golden parity
    - M-06.8 (conftest marker dup): no functional impact, close as wont-fix

    **Deferred to v1.2 (8):**
    - M-06.1 (handler order dependency)
    - M-06.2 (SVA-E002 dual exit code)
    - M-06.9 (deferred imports)
    - M-06.10 (GitHub URL inconsistency)
    - L-06.1 (exception type name swallowed)
    - L-06.2 (error codes in message strings)
    - L-06.8 (--version flag position)
    - L-06.9 (fixture shadowing)

    Each deferred item gets a one-line rationale. Append "Out of Scope for v1.1" section to `.planning/PROJECT.md` listing all deferred items with rationales.
    Commit: `docs(POLISH-02/03): MEDIUM/LOW triage — 19 findings resolved`
  </action>
  <acceptance_criteria>
    - 06-TRIAGE.md has 19 entries, each with disposition and rationale
    - PROJECT.md has "Out of Scope for v1.1" section
    - Zero open HIGH or MEDIUM findings remain
  </acceptance_criteria>
</task>

<task id="6.1.6" title="POLISH-04 — Cross-phase code review of v1.1 hardening diff">
  <read_first>.planning/phases/01/ through 05/ (SUMMARY.md files for change inventory)</read_first>
  <action>
    Review the complete v1.1 hardening diff (`git diff v1.0-tag..HEAD` or equivalent baseline against current HEAD):
    
    1. Review all changed src/sva2rtl/*.py files for:
       - Logic errors introduced by Phase 3 template refactor
       - Regression in Phase 4 _DECLARATIONS.clear() / _build_seq_repetition / _collect_signals
       - CLI behavior changes in Phase 5 (no breaking changes)
    2. Review all changed templates/*.j2 files for:
       - Macro consistency (all 11 templates use _macros.sv.j2)
       - HARDEN-01 fix applied via _attempt_fired_macro.sv.j2
       - No template has raw SV/V2001 duplication
    3. Verify:
       - 736 tests pass (721 + 15 golden mismatch known, 17 simulation skipped)
       - ruff + mypy --strict clean on src/
       - No new warnings in test output
    4. Write review to `.planning/phases/06-cleanup-version-sync-final-review/06-REVIEW.md`:
       - Summary of files reviewed
       - Verdict: "Zero new HIGH-severity findings"
       - Any observations (non-blocking notes)
    Commit: `docs(POLISH-04): cross-phase code review — zero new HIGH findings`
  </action>
  <acceptance_criteria>
    - 06-REVIEW.md exists with explicit "zero new HIGH findings"
    - Full test suite passes
    - ruff + mypy clean
  </acceptance_criteria>
</task>

## must_haves (Goal-Backward Verification)

- [ ] `__init__.py` and `pyproject.toml` both read `1.1.0`
- [ ] 06-TRIAGE.md lists all 19 findings with dispositions; PROJECT.md has "Out of Scope" section
- [ ] 06-REVIEW.md states "zero new HIGH-severity findings"
- [ ] 736 tests pass (same count as baseline); ruff + mypy clean
- [ ] Zero HIGH or MEDIUM findings remain open
- [ ] 3 doc fixes applied (M-06.4, M-06.5, L-06.6)
