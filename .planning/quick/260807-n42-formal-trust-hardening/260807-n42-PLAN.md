---
quick_id: 260807-n42
slug: formal-trust-hardening
mode: quick-full-inline
date: 2026-08-07
status: complete
must_haves:
  truths:
    - Only a completed PROVEN verification exits zero from the verification command.
    - Formal evidence records the actual relocatable tool invocation and enough tool identity to detect replay drift without leaking personal paths.
    - The formal workflow accepts and preserves the reviewed structured slang project context used by the monitor frontend.
    - A copied evidence bundle remains self-contained for includes, filelists, libraries, packages, defines, and parameter overrides within the supported project subset.
    - Missing open-liveness capability is reported before execution and a pinned Linux path is documented and tested.
    - Public progress, support, and industrial-gap ledgers agree with the current exact-SHA qualification boundary.
  artifacts:
    - src/sva2rtl/formal_cli.py
    - src/sva2rtl/formal_flow.py
    - tests/test_formal_cli.py
    - tests/test_formal_flow.py
    - tests/test_formal_project_context.py
    - tests/external_corpus/opentitan/
    - SUPPORT_MATRIX.md
    - INDUSTRIAL_VALIDATION_GAPS.md
  key_links:
    - Formal CLI options map into one validated project context used for both slang elaboration and evidence materialization.
    - result.json replay commands are derived from the executed configuration and checked against manifest tool identity.
    - CI and documentation tests prevent UNKNOWN/compile-only success and stale evidence claims from returning.
---

# Quick Task 260807-n42: Formal Trust Hardening

## Goal

Close the high-priority trust gaps found in the 2026-08-07 review without
expanding unsupported SVA semantics: tighten the result contract, make formal
project inputs replayable, add portable liveness diagnostics, and synchronize
the public evidence ledger.

## Task 1: Fail-closed result and replay contract

**Files:** `src/sva2rtl/formal_cli.py`, `src/sva2rtl/formal_flow.py`,
`tests/test_formal_cli.py`, `tests/test_formal_flow.py`,
`tools/ci/smoke_distribution.py`, related schema/documentation tests.

**Action:** Ensure the verification command returns zero only for `PROVEN`;
preserve bundle-only generation through an explicit compile command or a
non-proof exit. Record sanitized actual tool argv, versions, and executable
identity in the manifest/result, and validate replay without requiring a
literal PATH token. Reject modified tool context and retain privacy boundaries.

**Verify:** CLI reproduction tests cover compile-only, every result status,
custom tool paths, changed tool identity, installed-wheel smoke, and evidence
certificate replay.

**Done:** No `UNKNOWN`, `UNSUPPORTED`, `TIMEOUT`, `FAILED`, or `ERROR` path can
exit zero from verification; custom-tool bundles are replayable and bound.

## Task 2: Real-project formal context and portable liveness

**Files:** `src/sva2rtl/frontend.py`, `src/sva2rtl/formal_cli.py`,
`src/sva2rtl/formal_flow.py`, formal project corpus and tests,
`FORMAL_VERIFICATION.md`, container/CI support files.

**Action:** Reuse `SlangCompilationContext` for DUT and property elaboration,
expose validated source/filelist/include/define/parameter/library/single-unit
options, materialize the transitive supported input closure into a relocatable
bundle, and hash all semantic inputs. Add a `doctor`/capability report and a
pinned Linux container command for Super Prove-backed liveness. Add a
license/provenance-marked representative multi-file corpus plus good/bad cases.

**Verify:** End-to-end tests run from a relocated bundle and cover relative
includes, packages, defines, parameter specialization, library lookup,
assertion isolation, missing-live-engine UNKNOWN, and doctor output.

**Done:** The documented structured subset works in the primary formal path
and its evidence no longer depends on original absolute source layout.

## Task 3: Evidence maturity and governance closure

**Files:** `SUPPORT_MATRIX.md`, `PROJECT_STATUS.md`,
`INDUSTRIAL_VALIDATION_GAPS.md`, `.planning/STATE.md`, Phase 20 verification,
machine-readable evidence ledger/tests, and maintainability documentation.

**Action:** Reconcile GSD stale state, record final exact-HEAD CI/nightly/Full
Formal runs, replace stale public-gap claims, generate/check row evidence from
machine-readable data, and close one deliberately narrow first `Fully
supported` row only if every applicable evidence link is concretely satisfied.
Document the next candidate and keep unbounded/multi-clock/X/Z/general-local
boundaries unchanged. Isolate new formal evidence helpers into focused modules
when it reduces `formal_flow.py` review risk without semantic churn.

**Verify:** Documentation consistency, support-ledger schema, privacy, full
pytest, both simulator selections, generated RTL gates, Full Formal selection,
package smoke, Ruff, strict mypy, lock and diff checks.

**Done:** GSD reports no stale phase, public ledgers match current evidence,
and any `Fully supported` promotion is mechanically justified rather than
inferred from workflow-wide green status.
