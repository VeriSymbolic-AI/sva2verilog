---
quick_id: 260807-n42
status: complete
date: 2026-08-07
---

# Quick Task 260807-n42 Summary

## Outcome

The formal CLI now returns zero only for a completed `PROVEN` result;
`--compile-only` emits a replayable `UNKNOWN` bundle and exits 11. Evidence
binds privacy-safe executable SHA-256 identities for slang, SBY, Yosys,
`yosys-smtbmc`, the configured solver, and Super Prove. Replay uses tool roles,
records the commands actually executed, and rejects incomplete or drifted proof
contracts.

The primary formal path now accepts structured filelists, includes, defines,
top parameters, libraries, property-only sources, and single-unit mode. Slang
resolves the project and emits self-contained DUT/property snapshots; only the
assertion-free DUT snapshot enters Yosys. Schema-v2 decomposition with project
context rejects until a schema-v3 dependency binding exists.

A pinned Linux OSS CAD Suite replay image and capability doctor cover the local
Super Prove gap. A provenance-marked OpenTitan `prim_flop_2sync` slice proves
the intended delay relationship and produces a trace for a one-flop latency
mutant. This external test found and closed two real semantic defects: slang-v11
leading `##N` delays were discarded, and custom reset observations were omitted
from monitor child connections.

## Verification

- Default/full local suite: 1752 passed, 3 conditional skips, 1 expected
  dynamically classified k-induction xfail.
- Verilator simulation: 174 passed, 1 reviewed skip.
- Full Formal selection: 222 passed, 2 local missing-Super-Prove skips, 1
  expected k-induction xfail.
- Generated RTL synthesis/strict lint: 133 passed.
- Fixed-seed differential: 16 passed per simulator; date-seeded slow
  differential: 1 passed per simulator.
- AST importer mutation: 123/123 covered valid mutants killed; 26 uncovered
  candidates remain outside the denominator.
- Wheel/sdist build, out-of-tree installed smoke, archive privacy, Ruff, strict
  mypy, support-ledger validation, whitespace, and source privacy checks passed.

## Evidence Boundary

Docker is unavailable on this host, so the new image recipe was SHA-pin and
contract tested but not locally built. Local macOS still lacks Super Prove; its
two liveness skips are not pass evidence. The worktree must receive fresh
exact-SHA CI, differential-nightly, and Full Formal qualification after commit.
No support row is promoted to Fully supported, and the OpenTitan slice is not a
CDC, metastability, OpenTitan, or industrial sign-off claim.
